# Meals Search App build with Vue3 + Vite + Tailwind Css


## Installation
1. Clone the repository
2. Run `npm install`
3. Run `npm run dev` to start the application at localhost

## Related API
https://www.themealdb.com/api.php
